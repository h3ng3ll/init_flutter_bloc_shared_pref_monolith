import 'dart:io';

import 'package:mason/mason.dart';

/// https://docs.brickhub.dev/hooks/

void run(HookContext context) {
  // final flagFile = File(
  //   '.mason_initialized',
  // );
  //
  // if (flagFile.existsSync()) {
  //   context.logger.err(
  //     'This project has already been initialized.',
  //   );
  //   context.logger.err(
  //     'Remove .mason_initialized if you REALLY want to re-run the template.',
  //   );
  //   exit(1);
  // }
}
